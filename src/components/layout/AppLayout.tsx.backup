import { Outlet } from 'react-router-dom';
import Sidebar from './Sidebar.tsx';
import AICompanion from '../ai/AICompanion.tsx';

export const AppLayout = () => {
  return (
    <div className="flex min-h-screen bg-background text-foreground overflow-x-hidden selection:bg-primary/30">
      <Sidebar />
      <AICompanion />
      <main className="flex-1 flex flex-col min-h-screen transition-all duration-500 ease-in-out relative md:ml-64">
        <div className="flex-1 p-6 lg:p-10">
          <Outlet />
        </div>
      </main>
    </div>
  );
};
